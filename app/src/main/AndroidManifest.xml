<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bluapp.detailview">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".Detail3Activity"
            android:label="Detail View 3"/>
        <activity
            android:name=".Main3Activity"
            android:label="Collection View"/>
        <activity
            android:name=".Detail2Activity"
            android:label="Detail View 2"/>
        <activity
            android:name=".Main2Activity"
            android:label="Activity 2"/>
        <activity
            android:name=".DetailActivity"
            android:label="Detail View 1"/>
        <activity
            android:name=".MainActivity"
            android:label="Activity 1"/>
        <activity
            android:name=".WelcomeActivity"
            android:label="Welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>